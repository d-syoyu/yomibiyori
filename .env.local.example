# ローカル開発用環境変数
# このファイルを .env.local にコピーして使用
# 本番の.envと切り替えて使用する

# === Database (Docker) ===
DATABASE_URL=postgresql://yomibiyori:devpassword@localhost:5433/yomibiyori_dev

# === Cache / Queue (Docker) ===
REDIS_URL=redis://localhost:6380
REDIS_RANKING_PREFIX=rankings:

# === Auth (本番Supabaseを使用) ===
# 認証は本番のSupabaseを使い続ける（ローカルでSupabase認証は複雑なため）
SUPABASE_URL=https://<your-project>.supabase.co
SUPABASE_ANON_KEY=<anon-key>
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>
SUPABASE_JWT_SECRET=<jwt-secret>
SUPABASE_PROJECT_REF=<project-ref>
SUPABASE_JWT_AUDIENCE=authenticated
SUPABASE_JWKS_URL=https://<project-ref>.supabase.co/auth/v1/keys
SUPABASE_REQUEST_TIMEOUT=5.0
SUPABASE_JWKS_CACHE_TTL=3600

# === Environment ===
APP_ENV=development
DEBUG=true

# === 不要な項目（ローカルでは無効化） ===
# SENTRY_DSN=
# POSTHOG_API_KEY=
# EXPO_ACCESS_TOKEN=
