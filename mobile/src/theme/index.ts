/**
 * よみびよりデザインシステム
 * モダン×和のテーマ
 */

import colors, { japaneseColors } from './colors';
import { fontFamily, fontSize, lineHeight, letterSpacing } from './typography';
import { spacing, borderRadius, shadow } from './spacing';

export const theme = {
  colors,
  japaneseColors,
  fontFamily,
  fontSize,
  lineHeight,
  letterSpacing,
  spacing,
  borderRadius,
  shadow,
};

export {
  colors,
  japaneseColors,
  fontFamily,
  fontSize,
  lineHeight,
  letterSpacing,
  spacing,
  borderRadius,
  shadow,
};

export default theme;
